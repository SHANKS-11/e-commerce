# FreshCart Frontend (React)

## Setup

```bash
npm install
npm run dev
```

## Notes
- API base URL: `https://ecommerce.routemisr.com`
- Auth token is stored in `localStorage` under key `token`.
- Cart endpoints require header `token`.

